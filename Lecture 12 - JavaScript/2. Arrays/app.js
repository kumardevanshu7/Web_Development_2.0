// --------------------------------------------------- ARRAYS -----------------------------------------

// arrays are Hetrogenous
// const randomArray = [true, false, 32678, 'Hello World']

// IMPORTANT NOTES : do it in a developer tool kits

// const colors =['green', 'orange', 'pink'];
// colors.length

//  ---------------------------------------------- Arrays Basic Method -------------------------------------
const colors = ["green", "orange", "pink", "black", "yellow"];

// ----------> Pushing Methods
// in the Last Position
// 1. pop()
// 2. push()

// in the First Position
// shift() - like a pop
// unshift() - like a push

// ----------> Other Arrays Method - Important Methods:
// 1. slice() - to cut the strings (Destructive)
// 2. splice() - Multi Tasking (start, delete from, insert)
// 3. reverse() - it can reverse the array
// 4. indexOf() - it can find the item index
// 5. includes() - it can check the item place and give the result in ture and false
// 6. concat() - merge the strings : arr2.concat(arr1) or arr2.concat(arr1, ['apple'])
// 7. join() - it can join the item in every index : arr.join('--->')

// 8. split() - it can return the array

// url.split('/');
// (5) ['http:', '', '127.0.01', 'users', 'profile.jpg']
